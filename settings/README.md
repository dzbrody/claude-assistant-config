Claude Settings Reference
Reference copies of configuration files. Do not commit files containing actual tokens or API keys.

Files
claude-settings.json — Reference .claude/settings.json with placeholders for secrets

claude-config.json — Reference ~/.claude.json with placeholders (NOT committed, see .gitignore)
