# Run Health Check

Run the local health check script to verify all system components are operational.

```bash
bash ~/.claude-assistant/scripts/health-check.sh
```

Checks: Claude CLI MCP servers · daily briefs directory · config repo status · scheduled task files · Google Drive and OneDrive mounts · logs directory.

After running, report any ✗ failures and suggest corrective actions.
