*.key
.pem
.env
.env.
claude-config.json
mcp-servers/openproject-mcp/

# Claude local settings — contains MCP API keys and personal paths; use .example as template
.claude/settings.local.json

# Private contact data (phone numbers, WhatsApp JIDs — injected at runtime)
scheduled-tasks/.people.private.md

# Daily briefing output — operational/private
daily_briefs/

# Playwright session artifacts
.playwright-mcp/

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
.terraform.lock.hcl
terraform.tfvars
*.tfplan
tfplan

# Logs
logs/

# WhatsApp session store
store/
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# ERP API credentials — never commit
infrastructure/axerp-api/.env
